export type TvdbFavorites = {
  series: number[];
  movies: number[];
  episodes: number[];
  artwork: number[];
  people: number[];
  lists: number[];
};

export type TvdbFavorite = {
  series: number;
  movies: number;
  episodes: number;
  artwork: number;
  people: number;
  lists: number;
};
